# AI 使用说明与来源记录

本文件记录本项目在开发过程中使用 AI 工具的情况，供使用者、贡献者和后续维护者参考。

> 分发范围：本文件是完整记录，随仓库发布但**不随扩展安装包分发**（已列入 `.vscodeignore`）。
> `README.md` 里有一节「关于 AI」的摘要，供浏览商店页面或仓库首页的读者了解概况。

## 摘要

- 本项目的**源码、测试、脚本、文档与图标基本全部由 AI 编码助手生成**，由维护者提出需求、逐项确认设计决策并验收发布。
- 运行时**不包含任何 AI 能力**：扩展不联网、不上传数据、不收集遥测、不执行外部命令。
- 发布流程中**不写入 AI 相关的 Git trailer**，理由见下文「关于 Git 提交署名」。
- 代码以 MIT 许可发布，维护者对内容负责。

## 工具与范围

| 项目 | 说明 |
| --- | --- |
| 工具 | DeepSeek 编码 agent（DSH，`@deepseek-ai/dsh`） |
| 工具版本 | `0.1.5-rc.1` |
| 模型 | **DeepSeek-V4.1-Flash**（DSH 模型目录里的条目名为 `DeepSeek-V41-Flash`） |
| 模型 API 标识 | `deepseek-flash` |
| 模型提供方 | `deepseek-official` |
| 推理强度 | `high` |
| 时间 | 2026-09-15 |
| 范围 | 需求讨论、算法设计、全部源码、测试用例、文档、图标、打包与发布流程 |

以上取值均可自行核对：API 标识、提供方与推理强度来自本机 `~/.dsh/settings.yaml` 的
`agent-default-model` 配置；显示名来自 DSH 内置的模型目录（`@deepseek-ai/dsh-llm-deepseek`，
其中 `deepseek-flash` 对应的条目名为 `DeepSeek-V41-Flash`）。


## 人负责什么

- 提出需求与产品定位：做一个 YAML 注释对齐扩展
- 逐项确认设计决策：
  - 做成真正的 VS Code 扩展，而不是命令行脚本
  - 对齐到**固定列**（而不是按缩进层级分组各对齐一列）
  - 列号由命令弹出输入框指定，并记住上次用过的值
  - 独占一行的注释也推到同一列（与行尾注释对齐）
  - 中日韩字符按显示宽度 2 计算
  - 触发方式为手动命令 + 快捷键
- 决定安全边界与默认值：如 `saveAfterAlign` 默认不自动保存、`minSpaces` 取 2
- 决定发布去向：是否上架、publisher ID、仓库名、是否做多语言界面
- 安装验收、决定何时发布
- 承担本项目的一切责任

## AI 负责什么

- `src/core.js`：对齐算法、显示宽度计算、引号与块标量识别
- `src/extension.js`：命令、格式化器、配置读取、保存策略
- `test/run-tests.js`、`test/extension-smoke.js`：共 84 项自动化测试
- `tools/preview.js`、`tools/make-icon.py`
- `icon/icon128.png`（由脚本生成）
- `README.md`、`CHANGELOG.md`、`PUBLISHING.md`、本文件
- `package.json`、`.vscodeignore`、`.gitignore`、GitHub Actions 工作流

## 验证到什么程度

**已验证**

- 84 项自动化测试全部通过（核心算法 41 项 + 扩展集成 43 项），`npm test` 可复现
- 关键安全行为有专门用例覆盖：块标量（`|` / `>-` / `|2`）内部的 `#`、引号内的 `#`、
  跨行引号标量、`#` 前无空白、`CRLF` 保留、幂等性
- 打包产物校验：VSIX 文件清单、包内代码与源码哈希一致

**未验证 / 需注意**

- 集成测试使用的是**模拟的 `vscode` 模块**，用于验证「命令注册 → 输入框取列号 → 文档写回」
  和格式化器注册这条链路；**它不能替代在真实 VS Code 中的手工验收**
- 因此，在真实编辑器中右键调用、自动保存、设为默认格式化器后的 `formatOnSave` 行为，
  需要由维护者手工确认

## 关于 Git 提交署名

本项目**不**在提交信息里添加 `Co-authored-by: <AI 工具>` 这类 trailer。原因是：

- 带有工具名的 co-author trailer 会干扰 DCO / 贡献者校验，也让版权归属变得含糊
- 它会污染 SLSA / Sigstore / SBOM 这类以「可追溯的人类作者」为前提的来源链路
- 对上游项目而言，AI 贡献规则各不相同，写死在提交元数据里容易踩到别人的策略

AI 的参与情况以**本文件**为准，而不是提交元数据。

## 关于版权与许可

AI 生成内容的版权归属在不同司法辖区尚无统一结论，本项目的立场是：

- 项目由人类主导设计、提出需求、确认决策并决定发布，责任归属明确
- 以 [MIT 许可](LICENSE) 发布；如你需要更严格的来源保证，请自行评估
